{
  "num_orders": 10,
  "document_types": ["Order"],
  "rate_limit_delay": 1.0,
  "output_dir": "downloads",
  "search_keywords": ["order"],
  "_comment": "Configuration for DAWSON Document Extractor",
  "_settings": {
    "num_orders": "Number of documents to download (can be overridden via command line)",
    "document_types": "List of document types to filter and download (e.g., ['Order'], ['closing', 'dismissal'])",
    "rate_limit_delay": "Delay in seconds between API requests (increase to be more conservative)",
    "output_dir": "Directory where PDFs and metadata will be saved",
    "search_keywords": "Keywords to search for documents (should match document_types for best results)"
  }
}
